export enum ECommand {
  start = '/start',
  schedule = '/schedule',
  subscribe = '/subscribe',
  unsubscribe = '/unsubscribe',
  authorize = '/authorize',
  notify = '/notify',
  dump = '/dump',
  logs = '/logs',
  drop = '/drop',
  templates = '/templates',
  week = '/week',
  delete = '/delete',
}
